{
    "cmake.configureArgs": [
        "-Dzely_REPO_DIR=/${env:zely}/repo"
    ],
    "cmake.generator": "Ninja",
    "cmake.buildDirectory": "${workspaceFolder}/.zely/build",
    "cmake.installPrefix": "${env:zely}/repo",
    "clangd.arguments": [
        "--compile-commands-dir=${workspaceFolder}/.zely/build",
        "--clang-tidy",
    ]
}