{
    "cmake.configureArgs": [
        "-Dori_REPO_DIR=/${env:ori}/repo"
    ],
    "cmake.generator": "Ninja",
    "cmake.buildDirectory": "${workspaceFolder}/.ori/build",
    "cmake.installPrefix": "${env:ori}/repo",
    "clangd.arguments": [
        "--compile-commands-dir=${workspaceFolder}/.ori/build",
        "--clang-tidy",
    ]
}